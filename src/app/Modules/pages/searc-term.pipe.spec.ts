import { SearcTermPipe } from './searc-term.pipe';

describe('SearcTermPipe', () => {
  it('create an instance', () => {
    const pipe = new SearcTermPipe();
    expect(pipe).toBeTruthy();
  });
});
